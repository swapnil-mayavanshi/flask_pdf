<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PDF Text Replacer - Smart Document Editor</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #0c0f1c 0%, #1a1d35 25%, #2d1b69 50%, #1a1d35 75%, #0c0f1c 100%);
      background-size: 400% 400%;
      animation: gradientShift 15s ease infinite;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      position: relative;
      overflow: hidden;
    }

    @keyframes gradientShift {
      0%, 100% { background-position: 0% 50%; }
      25% { background-position: 100% 50%; }
      50% { background-position: 50% 0%; }
      75% { background-position: 50% 100%; }
    }

    /* Floating particles background */
    .particles {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 1;
    }

    .particle {
      position: absolute;
      width: 4px;
      height: 4px;
      background: rgba(35, 166, 213, 0.3);
      border-radius: 50%;
      animation: float 6s infinite ease-in-out;
    }

    .particle:nth-child(2n) {
      background: rgba(112, 40, 228, 0.3);
      animation-duration: 8s;
      animation-delay: -2s;
    }

    .particle:nth-child(3n) {
      background: rgba(0, 198, 255, 0.2);
      animation-duration: 10s;
      animation-delay: -4s;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) translateX(0px) scale(1); opacity: 0.3; }
      25% { transform: translateY(-20px) translateX(10px) scale(1.1); opacity: 0.6; }
      50% { transform: translateY(-40px) translateX(-5px) scale(0.9); opacity: 0.8; }
      75% { transform: translateY(-20px) translateX(-10px) scale(1.05); opacity: 0.4; }
    }

    .main-wrapper {
      display: flex;
      gap: 40px;
      max-width: 1400px;
      width: 100%;
      height: 90vh;
      position: relative;
      z-index: 10;
      animation: containerEntry 0.8s ease-out;
    }

    @keyframes containerEntry {
      from {
        opacity: 0;
        transform: translateY(30px) scale(0.95);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    .left-panel {
      flex: 1;
      background: rgba(15, 20, 35, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 35px;
      box-shadow:
        0 8px 32px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      position: relative;
      display: flex;
      flex-direction: column;
      height: 100%;
    }

    .right-panel {
      flex: 1;
      background: rgba(15, 20, 35, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 35px;
      box-shadow:
        0 8px 32px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      position: relative;
      display: flex;
      flex-direction: column;
      height: 100%;
    }

    .left-panel::before,
    .right-panel::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, #23a6d5, #7028e4, transparent);
      opacity: 0.6;
    }

    .header {
      text-align: center;
      margin-bottom: 32px;
      position: relative;
    }

    .app-icon {
      font-size: 3rem;
      background: linear-gradient(135deg, #23a6d5, #7028e4);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 16px;
      animation: iconPulse 3s ease-in-out infinite;
      display: inline-block;
    }

    @keyframes iconPulse {
      0%, 100% { transform: scale(1) rotate(0deg); }
      50% { transform: scale(1.1) rotate(2deg); }
    }

    h2 {
      background: linear-gradient(135deg, #23a6d5, #7028e4, #00c6ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      font-size: clamp(1.5rem, 3vw, 2rem);
      font-weight: 700;
      margin-bottom: 8px;
      letter-spacing: 0.5px;
      animation: titleGlow 2s ease-in-out infinite alternate;
    }

    @keyframes titleGlow {
      from { filter: drop-shadow(0 0 5px rgba(35, 166, 213, 0.3)); }
      to { filter: drop-shadow(0 0 15px rgba(112, 40, 228, 0.5)); }
    }

    .subtitle {
      color: #8892b0;
      font-size: 0.9rem;
      margin-bottom: 8px;
    }

    /* Status message styling */
    .status-message {
      margin-bottom: 24px;
      padding: 16px;
      border-radius: 12px;
      font-weight: 500;
      text-align: center;
      animation: statusSlide 0.5s ease-out;
      border: 1px solid;
      position: relative;
      overflow: hidden;
    }

    @keyframes statusSlide {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .status-success {
      background: linear-gradient(135deg, rgba(34, 197, 94, 0.1), rgba(16, 185, 129, 0.1));
      color: #10b981;
      border-color: rgba(16, 185, 129, 0.3);
    }

    .status-error {
      background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(220, 38, 38, 0.1));
      color: #ef4444;
      border-color: rgba(239, 68, 68, 0.3);
    }

    .status-warning {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(217, 119, 6, 0.1));
      color: #f59e0b;
      border-color: rgba(245, 158, 11, 0.3);
    }

    /* File section */
    .file-section {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .section-label {
      color: #64748b;
      font-weight: 600;
      margin-bottom: 12px;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .section-icon {
      color: #23a6d5;
      font-size: 1rem;
    }

    .file-drop-wrapper {
      position: relative;
      width: 100%;
      flex: 1;
      min-height: 300px;
      border: 2px dashed rgba(35, 166, 213, 0.4);
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(35, 166, 213, 0.05), rgba(112, 40, 228, 0.05));
      color: #e2e8f0;
      text-align: center;
      cursor: pointer;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      position: relative;
      overflow: hidden;
    }

    .file-drop-wrapper::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
      transition: left 0.6s ease;
    }

    .file-drop-wrapper:hover::before {
      left: 100%;
    }

    .file-drop-wrapper:hover {
      border-color: #23a6d5;
      border-style: solid;
      background: linear-gradient(135deg, rgba(35, 166, 213, 0.1), rgba(112, 40, 228, 0.1));
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(35, 166, 213, 0.2);
    }

    .file-drop-wrapper.active-drop {
      border-color: #7028e4;
      border-style: solid;
      background: linear-gradient(135deg, rgba(112, 40, 228, 0.15), rgba(35, 166, 213, 0.15));
      transform: scale(1.02);
      box-shadow: 0 12px 40px rgba(112, 40, 228, 0.3);
    }

    .file-drop-wrapper input[type="file"] {
      position: absolute;
      width: 100%;
      height: 100%;
      opacity: 0;
      cursor: pointer;
      z-index: 2;
    }

    .file-drop-icon {
      font-size: 3.5rem;
      background: linear-gradient(135deg, #23a6d5, #7028e4);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 20px;
      animation: iconBounce 2s ease-in-out infinite;
    }

    @keyframes iconBounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    .file-drop-text {
      font-size: 1.2rem;
      font-weight: 500;
      color: #cbd5e1;
      margin-bottom: 12px;
    }

    .file-drop-subtext {
      font-size: 0.9rem;
      color: #64748b;
      margin-bottom: 16px;
    }

    .file-name {
      font-weight: 600;
      color: #10b981;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-top: 12px;
      padding: 12px 20px;
      background: rgba(16, 185, 129, 0.1);
      border-radius: 12px;
      border: 1px solid rgba(16, 185, 129, 0.2);
      word-break: break-all;
      max-width: 100%;
    }

    /* Form inputs */
    .form-content {
      display: flex;
      flex-direction: column;
      gap: 24px;
      height: 100%;
    }

    .input-group {
      margin-bottom: 20px;
    }

    .input-label {
      color: #94a3b8;
      margin-bottom: 8px;
      font-weight: 500;
      font-size: 0.9rem;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .input-wrapper {
      position: relative;
    }

    .input-icon {
      position: absolute;
      left: 14px;
      top: 50%;
      transform: translateY(-50%);
      color: #64748b;
      font-size: 1rem;
      z-index: 1;
    }

    input[type="text"] {
      width: 100%;
      padding: 16px 16px 16px 44px;
      border-radius: 12px;
      border: 2px solid rgba(35, 166, 213, 0.2);
      background: rgba(15, 23, 42, 0.8);
      color: #e2e8f0;
      font-size: 1rem;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
    }

    input[type="text"]:focus {
      outline: none;
      border-color: #23a6d5;
      box-shadow: 0 0 0 4px rgba(35, 166, 213, 0.1);
      background: rgba(15, 23, 42, 0.9);
    }

    input[type="text"]:focus + .input-icon {
      color: #23a6d5;
    }

    /* Submit button */
    .submit-btn {
      width: 100%;
      background: linear-gradient(135deg, #23a6d5 0%, #7028e4 100%);
      color: #fff;
      border: none;
      border-radius: 12px;
      padding: 18px 24px;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
      margin-top: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
    }

    .submit-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.6s ease;
    }

    .submit-btn:hover::before {
      left: 100%;
    }

    .submit-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(35, 166, 213, 0.4);
      background: linear-gradient(135deg, #1e90ff 0%, #8a2be2 100%);
    }

    .submit-btn:active {
      transform: translateY(0) scale(0.98);
    }

    .btn-icon {
      font-size: 1.2rem;
      animation: btnIconSpin 2s linear infinite paused;
    }

    .submit-btn:hover .btn-icon {
      animation-play-state: running;
    }

    @keyframes btnIconSpin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    /* Download section */
    .download-section {
      margin-top: 24px;
      padding: 20px;
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(5, 150, 105, 0.1));
      border-radius: 16px;
      border: 1px solid rgba(16, 185, 129, 0.2);
      text-align: center;
      animation: downloadAppear 0.5s ease-out;
    }

    @keyframes downloadAppear {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    .download-btn {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      background: linear-gradient(135deg, #10b981, #059669);
      color: white;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.3s ease;
      margin-top: 16px;
    }

    .download-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
    }

    /* Info section */
    .info {
      padding: 24px;
      background: rgba(30, 41, 59, 0.6);
      border-radius: 16px;
      border: 1px solid rgba(51, 65, 85, 0.6);
      backdrop-filter: blur(10px);
      margin-top: auto;
    }

    .info-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
      color: #23a6d5;
      font-weight: 600;
      font-size: 1.1rem;
    }

    .info-content {
      color: #94a3b8;
      line-height: 1.6;
      font-size: 0.95rem;
    }

    .info-list {
      list-style: none;
      margin: 16px 0 0 0;
    }

    .info-list li {
      display: flex;
      align-items: center;
      gap: 12px;
      margin: 10px 0;
      color: #cbd5e1;
      font-size: 0.9rem;
    }

    .info-list li::before {
      content: '✦';
      color: #23a6d5;
      font-size: 0.9rem;
      flex-shrink: 0;
    }

    /* Fullscreen drop overlay */
    #fullscreen-drop-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(12, 15, 28, 0.95);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      z-index: 9999;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      pointer-events: none;
      opacity: 0;
      transition: all 0.3s ease;
      border: 12px dashed transparent;
      box-sizing: border-box;
    }

    #fullscreen-drop-overlay.visible {
      opacity: 1;
      pointer-events: all;
      border-color: #23a6d5;
      animation: overlayPulse 2s infinite ease-in-out;
    }

    @keyframes overlayPulse {
      0%, 100% {
        border-color: #23a6d5;
        box-shadow: inset 0 0 50px rgba(35, 166, 213, 0.1);
      }
      50% {
        border-color: #7028e4;
        box-shadow: inset 0 0 80px rgba(112, 40, 228, 0.2);
      }
    }

    .drop-text {
      font-size: clamp(2.5rem, 8vw, 5rem);
      font-weight: 700;
      background: linear-gradient(135deg, #23a6d5, #7028e4, #00c6ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      text-shadow: 0 0 30px rgba(35, 166, 213, 0.5);
      animation: dropTextPulse 2s infinite ease-in-out;
      text-align: center;
      margin-bottom: 24px;
    }

    @keyframes dropTextPulse {
      0%, 100% { transform: scale(1) rotateX(0deg); }
      50% { transform: scale(1.05) rotateX(5deg); }
    }

    .drop-icon {
      font-size: 4rem;
      color: #23a6d5;
      margin-bottom: 16px;
      animation: dropIconFloat 3s ease-in-out infinite;
    }

    @keyframes dropIconFloat {
      0%, 100% { transform: translateY(0) rotate(0deg); }
      50% { transform: translateY(-10px) rotate(5deg); }
    }

    /* Responsive design */
    @media (max-width: 1200px) {
      .main-wrapper {
        flex-direction: column;
        gap: 20px;
        height: auto;
        max-height: 95vh;
        overflow-y: auto;
      }

      .left-panel,
      .right-panel {
        height: auto;
        min-height: 400px;
      }

      .file-drop-wrapper {
        min-height: 200px;
      }
    }

    @media (max-width: 768px) {
      body {
        padding: 10px;
      }

      .main-wrapper {
        gap: 15px;
        height: auto;
        max-height: 95vh;
      }

      .left-panel,
      .right-panel {
        padding: 20px;
        min-height: 350px;
      }

      .file-drop-wrapper {
        min-height: 150px;
        padding: 12px;
      }

      .file-drop-icon {
        font-size: 2.5rem;
      }

      .app-icon {
        font-size: 2rem;
      }

      .input-group {
        margin-bottom: 12px;
      }
    }

    /* Loading animation */
    .loading {
      display: inline-block;
      width: 20px;
      height: 20px;
      border: 3px solid rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      border-top-color: #fff;
      animation: spin 1s ease-in-out infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>

<!-- Floating particles background -->
<div class="particles" id="particles"></div>

<!-- Fullscreen overlay for drag-and-drop -->
<div id="fullscreen-drop-overlay">
  <div class="drop-icon">
    <i class="fas fa-cloud-upload-alt"></i>
  </div>
  <div class="drop-text">Drop Your PDF Here!</div>
  <div style="color: #64748b; font-size: 1.2rem; text-align: center;">
    Release to upload and process your document
  </div>
</div>

<div class="main-wrapper">
  <!-- Left Panel - File Upload -->
  <div class="left-panel">
    <div class="header">
      <div class="app-icon">
        <i class="fas fa-file-pdf"></i>
      </div>
      <h2>PDF Text Replacer</h2>
      <div class="subtitle">Smart Document Editor</div>
    </div>

    <div class="file-section">
      <div class="section-label">
        <i class="fas fa-file-upload section-icon"></i>
        Upload Document
      </div>
      <div class="file-drop-wrapper" id="file-drop-wrapper">
        <div class="file-drop-icon">
          <i class="fas fa-file-pdf"></i>
        </div>
        <div class="file-drop-text" id="file-drop-text">
          Click or drag your PDF here
        </div>
        <div class="file-drop-subtext">
          Supports PDF files up to 50MB
        </div>
        <input type="file" name="data_file" id="data_file" accept=".pdf" required>
      </div>
    </div>
  </div>

  <!-- Right Panel - Form and Info -->
  <div class="right-panel">
    <div class="form-content">
      <!-- Status message would go here -->
      <div id="status-container" style="display: none;">
        <div class="status-message" id="status-message"></div>
      </div>

      <!-- Download section would go here -->
      <div id="download-container" style="display: none;">
        <div class="download-section">
          <div style="color: #10b981; font-weight: 600; margin-bottom: 8px;">
            <i class="fas fa-check-circle" style="margin-right: 8px;"></i>
            Processing Complete!
          </div>
          <div style="color: #64748b; margin-bottom: 16px;">
            Successfully replaced <span id="replaced-count">0</span> occurrence<span id="plural-s">s</span>
          </div>
          <a href="#" class="download-btn" id="download-link">
            <i class="fas fa-download"></i>
            Download Processed PDF
          </a>
        </div>
      </div>

      <form method="post" enctype="multipart/form-data" id="mainForm">
        <div class="input-group">
          <div class="input-label">
            <i class="fas fa-search section-icon"></i>
            Text to Replace
          </div>
          <div class="input-wrapper">
            <input type="text" name="search_str" id="search_str" placeholder="Enter word or phrase to find..." required>
            <i class="fas fa-search input-icon"></i>
          </div>
        </div>

        <div class="input-group">
          <div class="input-label">
            <i class="fas fa-edit section-icon"></i>
            Replacement Text
          </div>
          <div class="input-wrapper">
            <input type="text" name="replace_str" id="replace_str" placeholder="Enter replacement text..." required>
            <i class="fas fa-edit input-icon"></i>
          </div>
        </div>

        <button type="submit" class="submit-btn" id="submitBtn">
          <i class="fas fa-magic btn-icon"></i>
          <span>Replace Text & Generate PDF</span>
        </button>
      </form>

      <div class="info">
        <div class="info-header">
          <i class="fas fa-info-circle"></i>
          How it works
        </div>
        <div class="info-content">
          This tool intelligently finds and replaces text in your PDF documents while preserving formatting and layout.
          <ul class="info-list">
            <li>Upload any PDF document</li>
            <li>Specify the text you want to replace</li>
            <li>Enter the new replacement text</li>
            <li>Download your updated PDF instantly</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Create floating particles
  function createParticles() {
    const particlesContainer = document.getElementById('particles');
    const particleCount = 50;

    for (let i = 0; i < particleCount; i++) {
      const particle = document.createElement('div');
      particle.className = 'particle';
      particle.style.left = Math.random() * 100 + '%';
      particle.style.top = Math.random() * 100 + '%';
      particle.style.animationDelay = Math.random() * 10 + 's';
      particle.style.animationDuration = (Math.random() * 10 + 5) + 's';
      particlesContainer.appendChild(particle);
    }
  }

  // Initialize particles
  createParticles();

  // File upload functionality
  const fileInput = document.getElementById('data_file');
  const fileDropText = document.getElementById('file-drop-text');
  const fileDropWrapper = document.getElementById('file-drop-wrapper');
  const overlay = document.getElementById('fullscreen-drop-overlay');
  const mainWrapper = document.querySelector('.main-wrapper');
  const mainForm = document.getElementById('mainForm');
  const submitBtn = document.getElementById('submitBtn');

  let dragCounter = 0;
  const originalDropText = fileDropText.innerHTML;

  // Prevent default drag behaviors
  function preventDefaults(e) {
    e.preventDefault();
    e.stopPropagation();
  }

  // Show fullscreen overlay
  function showOverlay() {
    if (dragCounter === 0) {
      overlay.classList.add('visible');
      mainWrapper.style.transform = 'scale(0.95)';
      mainWrapper.style.opacity = '0.7';
      fileDropWrapper.classList.add('active-drop');
    }
    dragCounter++;
  }

  // Hide fullscreen overlay
  function hideOverlay() {
    dragCounter--;
    if (dragCounter <= 0) {
      dragCounter = 0;
      overlay.classList.remove('visible');
      mainWrapper.style.transform = 'scale(1)';
      mainWrapper.style.opacity = '1';
      fileDropWrapper.classList.remove('active-drop');
    }
  }
